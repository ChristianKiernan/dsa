# {{Problem Title}}

## Pseudocode

1. {{High-level base condition / early return}}

2. Initialize required data structures:
    - {{Structure 1}} → {{purpose}}
    - {{Structure 2}} → {{purpose}}

3. For each index `i` from `0` to `{{n}} - 1`:
   3a. Process element from first input  
   - If key does not exist, initialize its value  
   - Update its value  
   3b. Process element from second input  
   - If key does not exist, initialize its value  
   - Update its value

4. For each key in {{primary data structure}}:
   4a. Compare values across structures  
   4b. If values are not equal, return **false**

5. If all checks pass, return **true**

---

## Complexity
- **Time:** {{O(...)}}
- **Space:** {{O(...)}}

---

## Edge Cases
- {{Edge case 1}}
- {{Edge case 2}}
- {{Edge case 3}}
- {{Edge case 4}}

---

## Notes
- {{Key insight / heuristic / pattern recognition}}
  EOF
